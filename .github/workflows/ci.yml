name: CI

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

# Prevent duplicate runs but don't cancel in-progress jobs
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  lint-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt', '**/requirements-dev.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install --no-cache-dir -r requirements.txt
        pip install --no-cache-dir -r requirements-dev.txt
        # Fail-safe: Explicitly install known missing deps
        pip install pandas gspread scikit-learn
        pip install ruff mypy pytest
        pip list

    - name: Lint with Ruff
      run: ruff check .

    - name: Type check with mypy
      run: mypy --config-file pyproject.toml application/ domain/ infrastructure/ config/
      continue-on-error: true

    - name: Run tests
      run: pytest tests/ -v
      continue-on-error: true
