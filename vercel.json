{
    "buildCommand": "chmod +x scripts/build.sh && ./scripts/build.sh",
    "outputDirectory": "dist",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.py"
        }
    ]
}
