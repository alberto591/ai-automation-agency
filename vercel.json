{
    "buildCommand": "cd appraisal-tool && npm install && npm run build && cd ../dashboard && npm install && npm run build && mkdir -p ../appraisal-tool/dist/dashboard && cp -r dist/* ../appraisal-tool/dist/dashboard/",
    "outputDirectory": "appraisal-tool/dist",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.py"
        }
    ]
}
